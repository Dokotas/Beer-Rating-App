.text-center.py-5
  %h1= t("app_name")
  %p.lead= t("pages.about_text")
  - if signed_in?
    = link_to t("nav.work"), work_path, class: "btn btn-primary btn-lg mt-3"
  - else
    = link_to t("nav.sign_up"), signup_path, class: "btn btn-success btn-lg mt-3"